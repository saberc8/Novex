# Codex App POC

Standalone web POC for a Codex-like developer agent workbench. This app is a front-end-only demo and does not use OpenAI or Codex brand assets.

## Commands

```bash
pnpm install
pnpm dev
pnpm test
pnpm typecheck
pnpm lint
pnpm build
```
