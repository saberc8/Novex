# Codex Migration Matrix

| Module | Codex Source | Novex Target | Mode | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Agent protocol | `codex-rs/protocol/src/items.rs`, `codex-rs/protocol/src/protocol.rs` | `crates/novex-agent-protocol` + RunEvent API | direct/adapt | slice-23 implemented | Turn item, tool call, observation, final answer vocabulary, and Codex-style run event stream facade with replayable `sequence_no` cursor are in place |
| Runtime loop | `codex-rs/core/src/session/turn.rs`, `codex-rs/core/src/compact*`, `codex-rs/core/src/tasks/*`, `codex-rs/core/src/state/turn.rs` | `crates/novex-agent-runtime` + `backend/src/application/ai/agent_service.rs` + `backend/src/application/ai/agent_queue_runtime.rs` | adapt | slice-69 implemented | Budget-bounded multi-turn model loop maps model output, single/batch tool calls, observations, context compaction, final answer, DB-backed cancellation checkpoints, active runtime-registry token interrupts, Codex-style supervised active run snapshots, replayable SSE event cursor, authenticated durable WebSocket run-event transport, short-lived browser WebSocket ticket handoff, durable `ai_agent_run_queue`, queued run creation, Postgres claim/lease worker polling, shared HTTP/worker runtime registry, deterministic existing-run execution, queued model-loop existing-run execution, pending/retrying queue-row cancellation sync, queued approval-resume requeue, RabbitMQ Agent queue wake-up message/topology contract, broker execute consumer with exact queue/tenant/run claim plus retry/dead routing, durable Agent queue outbox publishing, request-level `modelRouteId` selection for configured CodeAgent routes, and a gated `pnpm smoke:agent-live` POC live smoke are in place; model-assisted context compaction, deterministic fallback on compaction model failure, remote compaction endpoint request/checkpoint contract, Codex-style compaction model request metadata envelope, Responses-compatible provider compaction request plan with `compaction_trigger`, Codex-style unary `/responses/compact` transport, background Responses response-id/status capture with `background=true`/`store=true` request mode and provider-call lease metadata persistence, JSON/SSE compaction output parser, persistent-run-status provider abort watcher, cancellation supervisor metadata, compaction/runtime-supervisor strategy tags for trace/eval, durable `ai_model_provider_call_lease` rows for tenant-bound chat/model calls, local provider-call context with run/source/purpose metadata, Agent trace `providerCallLeaseId` links, tenant-scoped provider-call lease list/expire-stale controls, provider-native cancel control with Responses `/responses/{id}/cancel` dispatch and durable local cancellation evidence, provider-call lease heartbeat refresh while provider awaits are active, tenant-bound embedding/rerank provider-call leases for RAG retrieval, tenant-bound media image provider-call leases for Agent generation, typed `AgentTurnItem` history projection for model-loop sampling, canonical single/batch tool-call prompt replay, observation prompt projection with call id/tool/status, latest-compaction-window sampling resume, durable `ai_agent_turn_item` ledger rows linked to source run events, transactional event-plus-item persistence, `turnItems` trace replay, CodeAgent chat-completions and configured Responses transport selection with output-text SSE token-delta parsing, streaming partial tool-call JSON parser, backend provider-stream tool-call detection into durable `model_stream_tool_call` inference events, streamed parsed tool-call output adoption for model-loop turn decisions, streamed tool-call early-stop of local provider await into the existing approval/execution path, early provider stream response id/status capture into stream events and early-stop completion metadata, provider stream lease id propagation, streamed tool-call early-stop provider-native remote cancellation dispatch from captured stream response metadata with trace/eval evidence, live provider stream channel drain into durable `model_delta` run events, final `model_inference` streaming metadata, provider-call lease delta-count evidence, model_delta inference trace preservation, streamed tool-call trace/eval tags, eval streaming tags, frontend model-delta rendering in Agent workspace/Codex POC, typed `ModelChatStreamCall` stream-native runtime API boundary with Agent model-loop consumption, provider-client-owned `ModelChatStreamCompletionBuilder` incremental chat stream completion assembly, `ModelChatStreamLifecycle` ownership of stream transport/events in the Agent await path, owned abort-on-drop `ModelChatStreamTransportTask` replacement for the public boxed response future, and private stream-specific transport executor normalization/route/fallback dispatch from `ModelChatStreamTransportTask`, explicit `ModelProviderDispatchMode` selection separates stream provider HTTP/SSE dispatch from unary response parsing at route execution, and a backend-local `model_provider_transport` adapter remains a compatibility facade for provider-client-backed HTTP/native-cancel/RAG/media dispatch plus chat/compaction parser error mapping while backend still owns route resolution, route execution, stream event emission, provider-call leases, and trace/eval context, backend compatibility submodules for generic HTTP, native cancel, RAG, and media dispatch are in place, and `novex-provider-client` now owns provider-neutral RAG response parsers, reusable provider HTTP client and POST primitives, embedding/rerank provider dispatch APIs, provider-native cancel dispatch APIs, media image provider dispatch APIs, provider response text reading, JSON/SSE chat output parsers, Responses compaction parsers, stream completion assembly, provider response id/status extraction, chat plan DTOs, transport enum, endpoint selection, payload construction, streamability predicate, chat dispatch request DTOs, stream dispatch API, and unary response-text dispatch API, with provider-client internals split into focused error, HTTP, chat planning, chat dispatch, chat parsing, compaction parsing, native cancel, media, and RAG modules behind the stable facade |
| Tool schema | `codex-rs/tools/src/*` | `crates/novex-tools` | direct/adapt | slice-1 implemented | ToolDefinition and model-visible tool schema are in place |
| Tool router | `codex-rs/core/src/tools/router.rs`, `codex-rs/core/src/tools/registry.rs` | `crates/novex-tools` | adapt | slice-4 implemented | Registry-owned model-visible specs, tool-call validation, unknown-tool stop reason, backend model-loop routing, executor registry vocabulary, deterministic executor lookup errors, default Agent model-loop executor bindings, backend registry lookup before model-loop tool execution, `executorBinding` event/audit metadata, shared executor dispatch plans, and backend dispatch-plan-guided tool I/O are in place; concrete executor extraction and parallel runtime remain next |
| Parallel tools | `codex-rs/core/src/tools/parallel.rs` | `crates/novex-tools` + backend model loop | adapt | slice-5 implemented | Shared/exclusive lock policy, cancellation-wait metadata, deterministic batch planning, parsed tool-call batches, backend batch event visibility, true parallel tool I/O, timeout-driven cancelled observations, and runtime-token external cancellation are in place; background join-handle control remains next |
| Rollout trace | `codex-rs/rollout*` | `crates/novex-trace` | adapt | slice-19 implemented | TraceBundle, replay API, `ai_rollout`, eval case capture, `trace_replay` eval gate, runtime span preservation for retrieval/action selection/context compaction/cancellation, runtime-supervisor cancellation evidence, remote compaction endpoint evidence, model inference spans for route/provider/model/latency/usage/cost, streaming model_delta inference spans, provider error spans, route-policy provider retry spans, route fallback lifecycle attempts, process-local route circuit breaker cooldown attempts, bounded multi-hop provider lifecycle attempts, persistent cross-process route circuit breaker state, tenant-scoped breaker list/clear controls, persistent-first runtime breaker checks, model ops summary aggregation for route/provider/health/usage/cost/latency/breaker state, persisted model health-check rows, scheduler builtin model health refresh, seeded default health-check job, active model ops health alerts, ops summary active-alert exposure for dashboards, scheduler-driven Feishu/dry-run alert delivery, and durable delivery/audit records are in place; frontend rendering, multi-channel escalation, and external alert transport hardening remain next |
| MCP | `codex-rs/codex-mcp`, `rmcp-client` | `crates/novex-mcp` | adapt | slice-1 implemented | Tenant-governed server registration, deterministic discovery, model-visible tool mapping, audit path, and mock/dry-run invocation are in place; live streaming MCP client remains next |
| Guardian | `codex-rs/core/src/guardian` | `crates/novex-approval-review` | adapt | slice-22 implemented | Structured fail-closed approval review contract, Codex-style denial circuit breaker, dedicated `guardian_review` model route purpose, compact transcript/action prompt, strict model assessment parser, timeout/provider/parse fail-closed decisions, backend approval-pause Guardian model payloads, explicit reviewed-approval continuation gate, Guardian auto-approved action evidence, trace preservation, and eval tags are in place; reusable Codex-style subagent review session manager, delta transcript cursor, and persistent denial-breaker integration remain next |
| Exec policy | `codex-rs/execpolicy`, `sandboxing`, `exec-server` | `services/sandbox-runner` | service adapt | deferred | No backend shell execution |

## Current Acceptance Evidence

Updated on 2026-06-18 from branch `feat/enterprise-agent-foundation`.

| Slice | Current acceptance evidence | Verification command |
| --- | --- | --- |
| Agent protocol | `crates/novex-agent-protocol` serializes turn items, tool calls, observations, and terminal outcomes | `cargo test -p novex-agent-protocol --offline` |
| Runtime loop POC | `runtimeMode=model_loop` uses configured CodeAgent routes, accepts optional `modelRouteId` for pinned tenant/env routes, applies the selected route to model sampling, retry policy, and model-assisted compaction, parses single and batch model-tool-call output, executes budget-bounded tool calls, records observations, compacts accumulated context after tool observations with deterministic fallback, records remote compaction endpoint request metadata, passes Codex-style compaction `requestMetadata` into model runtime calls, can route compatible compaction calls through Responses-style provider payloads with `compaction_trigger`, background/store Responses request mode, explicit unary `/responses/compact` payloads, JSON/SSE compaction output parsing, and provider response id/status capture into provider-call leases, records supervised active run snapshots/cancel metadata, exposes replayable SSE event stream cursor, authenticated durable WebSocket run-event transport, and browser-safe short-lived WebSocket ticket handoff helpers, actively aborts provider awaits when another process persists run cancellation, creates durable provider-call lease rows for tenant-bound chat/model calls, refreshes provider-call lease heartbeats while provider awaits are active, records tenant-bound embedding/rerank provider-call leases for RAG retrieval, records tenant-bound media image provider-call leases for Agent generation, links Agent inference traces to `providerCallLeaseId`, exposes provider-call lease list/expire-stale/cancel controls with durable local cancellation evidence, projects typed `AgentTurnItem` history into provider sampling messages with latest-compaction-window resume, persists runtime turn items into durable `ai_agent_turn_item` rows with source event linkage, returns ordered replay items as `turnItems`, streams CodeAgent chat-completions and configured Responses output-text SSE token deltas into durable `model_delta` run events, records final `model_inference` streaming metadata, preserves model_delta chunks in trace/eval with `streamingModelOutput` tags, detects complete streamed tool-call JSON into `model_stream_tool_call` inference spans with `streamingToolCallDetected` eval tags, adopts streamed parsed tool-call output for model-loop turn decisions, early-stops the local provider await when streamed tool-call output is complete and immediately enters the existing approval/execution path, captures provider stream response id/status into model_delta/model_stream_tool_call events and early-stop completion metadata, propagates provider-call lease ids through provider stream events, dispatches provider-native remote cancellation after streamed tool-call early-stop using captured stream response metadata through the provider-client-backed backend adapter, records native-cancel trace/eval evidence, renders model_delta chunks as live model output in Agent Workspace and Codex POC, consumes typed `ModelChatStreamCall` transport task/event receiver from the model runtime instead of injecting provider stream senders in Agent loop, assembles streamed chat completions through provider-client-owned `ModelChatStreamCompletionBuilder` while provider events are emitted, owns stream transport/events through `ModelChatStreamLifecycle` in the Agent await path, wraps stream provider work in an abort-on-drop `ModelChatStreamTransportTask`, runs that task through a private stream-specific executor instead of the public unary facade, selects explicit stream provider dispatch mode at route execution, builds chat/Responses endpoint/payload/transport plans through provider-client-owned chat request planning APIs, sends chat/Responses provider HTTP requests through provider-client-owned stream/unary dispatch APIs, parses provider text/SSE outputs through provider-client-owned chat/compaction parser APIs, sends provider-native cancel requests through provider-client, and runs configured RAG embedding/rerank plus media image provider calls through provider-client-backed adapters, keeps provider-client primitives organized behind focused error, HTTP, chat planning, chat dispatch, chat parsing, compaction, native cancel, media, and RAG modules, keeps sampling until final answer, approval pause, runtime-registry cancellation token, external cancellation checkpoint, persistent provider-abort cancellation, streamed tool-call early-stop, or budget stop, and has a gated live smoke command that creates a run and requires `model_inference` event evidence | `cargo test -p novex-agent-runtime --offline && cargo test -p novex-agent-runtime remote_compaction --offline && cargo test -p novex-agent-runtime streaming_parser --offline && cargo test -p backend-rust model_chat_payload --offline && cargo test -p backend-rust provider_token_delta --offline && cargo test -p backend-rust provider_stream_response_id --offline && cargo test -p backend-rust provider_stream_lease_id --offline && cargo test -p backend-rust provider_stream_native_cancel --offline && cargo test -p backend-rust provider_responses_transport --offline && cargo test -p backend-rust model_delta --offline && cargo test -p backend-rust model_delta_spans --offline && cargo test -p backend-rust provider_stream_tool_call --offline && cargo test -p backend-rust model_stream_tool_call --offline && cargo test -p backend-rust streamed_tool_call_output --offline && cargo test -p backend-rust streamed_tool_call_decision --offline && cargo test -p backend-rust streamed_tool_call_early_stop --offline && cargo test -p novex-eval streaming_tool_call --offline && cargo test -p novex-eval provider_native_cancel --offline && cargo test -p backend-rust model_stream_native_runtime_api --offline && cargo test -p backend-rust model_stream_completion_builder --offline && cargo test -p backend-rust model_stream_call_lifecycle --offline && cargo test -p backend-rust model_stream_transport_task --offline && cargo test -p backend-rust model_stream_transport_executor --offline && cargo test -p backend-rust model_provider_stream_dispatch_mode --offline && cargo test -p backend-rust model_provider_stream_dispatch_route_path --offline && cargo test -p backend-rust model_provider_http_transport_adapter --offline && cargo test -p backend-rust model_provider_response_transport_adapter --offline && cargo test -p backend-rust provider_client_chat_response_parsers_live_in_provider_client_crate --offline && cargo test -p backend-rust provider_client_chat_dispatch_lives_in_provider_client_crate --offline && cargo test -p backend-rust provider_client_chat_request_plan_lives_in_provider_client_crate --offline && cargo test -p novex-provider-client --offline && cargo test -p backend-rust provider_client_crate_uses_focused_modules --offline && cargo test -p backend-rust model_provider_native_cancel_transport_adapter --offline && cargo test -p backend-rust provider_client_native_cancel_dispatch_lives_in_provider_client_crate --offline && cargo test -p backend-rust model_provider_rag_transport_adapter --offline && cargo test -p backend-rust model_provider_media_transport_adapter --offline && cargo test -p backend-rust provider_client_media_dispatch_lives_in_provider_client_crate --offline && cargo test -p backend-rust provider_compact_transport --offline && cargo test -p backend-rust provider_background_response_capture --offline && cargo test -p backend-rust provider_compact_unary --offline && cargo test -p backend-rust provider_call_lease --offline && cargo test -p backend-rust provider_call_lease_heartbeat --offline && cargo test -p backend-rust provider_call_lease_controls --offline && cargo test -p backend-rust provider_call_lease_cancel --offline && cargo test -p backend-rust runtime_embedding --offline && cargo test -p backend-rust rerank_ --offline && cargo test -p backend-rust media_ --offline && cargo test -p backend-rust model_loop_history_messages --offline && cargo test -p backend-rust agent_turn_item --offline && cargo test -p backend-rust agent_service_response_item_ledger --offline && cargo test -p backend-rust agent_service_model_loop_installs_response_item_history_for_sampling --offline && cargo test -p backend-rust agent_poc_configured_model_route --offline && cargo test -p backend-rust agent_event_stream --offline && cargo test -p backend-rust agent_event_ws_ticket --offline && cargo test -p backend-rust agent_event_websocket --offline && cargo test -p backend-rust agent_event_websocket_ticket --offline && cargo test -p backend-rust remote_compaction --offline && cargo test -p backend-rust runtime_supervisor --offline && cargo test -p backend-rust runtime_registry --offline && cargo test -p backend-rust external_cancel --offline && cargo test -p backend-rust provider_abort --offline && cargo test -p backend-rust model_loop_compaction --offline && cargo test -p backend-rust model_loop --offline && cd apps/agent-workspace && pnpm test -- src/api/agent.test.ts app/page.test.tsx src/lib/agent-events.test.ts && cd ../codex-app-poc && pnpm test -- src/api/agent.test.ts app/page.test.tsx src/lib/agent-events.test.ts scripts/agent-live-smoke.test.mjs`; live provider check: `NOVEX_LIVE_AGENT_SMOKE=1 NEXT_PUBLIC_API_BASE_URL=http://localhost:4398 NEXT_PUBLIC_AGENT_MODEL_ROUTE_ID=<route> pnpm smoke:agent-live` |
| Agent background run queue | `executionMode=queued` creates queued Run Graph records and `ai_agent_run_queue` rows; worker config, spawn, claim, lease, retry/fail/cancel/succeed/waiting-approval status updates, shared Agent runtime registry, deterministic existing-run execution, model-loop existing-run execution, pending/retrying queue-row cancellation sync, queued approval resume requeue, Agent RabbitMQ wake-up topology/message publisher contract, Agent broker execute consumer, and durable Agent queue outbox publisher are wired | `cargo test -p backend-rust agent_run_queue --offline && cargo test -p backend-rust agent_queue_cancel_sync --offline && cargo test -p backend-rust agent_queue_resume_requeue --offline && cargo test -p backend-rust agent_queue_broker_wakeup --offline && cargo test -p backend-rust agent_queue_broker_consumer --offline && cargo test -p backend-rust agent_queue_outbox --offline && cargo test -p backend-rust rabbitmq --offline && cargo test -p backend-rust agent_run_command --offline && cargo test -p backend-rust agent_service_queued_run_creation --offline && cargo test -p backend-rust agent_service_queued_execution --offline && cargo test -p backend-rust queued_model_loop --offline && cargo test -p backend-rust agent_queue_runtime --offline` |
| Tool router | `novex-tools` owns built-in agent tool definitions, model-visible specs, duplicate/unknown validation, executor registry vocabulary, deterministic executor lookup errors, default Agent model-loop executor bindings, and executor dispatch plans; backend model-loop resolves routed tool calls through the registry before DB lookup/execution, records `executorBinding` in action/audit payloads, and uses dispatch-plan dependency flags plus executor-code dispatch before legacy tool-code fallback | `cargo test -p novex-tools tool_executor_registry --offline && cargo test -p novex-tools tool_executor_dispatch_plan --offline && cargo test -p novex-tools --offline && cargo test -p backend-rust tool_executor_registry --offline && cargo test -p backend-rust model_loop_tool_executor_binding_payload --offline && cargo test -p backend-rust tool_executor_dispatch_plan --offline && cargo test -p backend-rust model_loop --offline` |
| Parallel tools | `novex-tools` exposes shared/exclusive locks, cancellation-wait metadata, deterministic batch planning, and backend `ActionSelected` `concurrencyPolicy`, `batchExecutionMode`, `toolCallBatch`, plus parallel tool I/O with serial audit/step persistence, timeout-driven `Cancelled` observations, and runtime-token external cancellation | `cargo test -p novex-tools --offline && cargo test -p backend-rust runtime_registry --offline && cargo test -p backend-rust tool_io_timeout --offline && cargo test -p backend-rust parallel_tool --offline && cargo test -p backend-rust model_loop --offline` |
| Codex POC UI | `apps/codex-app-poc` sends real Agent run requests with `runtimeMode=model_loop`, can include `NEXT_PUBLIC_AGENT_MODEL_ROUTE_ID` as `modelRouteId`, fetches run-event snapshots after run creation, renders `model_delta` chunks as live model output, and exposes `pnpm smoke:agent-live` for configured-model live acceptance | `cd apps/codex-app-poc && pnpm test -- src/api/agent.test.ts app/page.test.tsx src/lib/agent-events.test.ts scripts/agent-live-smoke.test.mjs && pnpm typecheck` |
| MCP gateway | MCP tools can be registered, discovered, converted to `ai_tool`, audited, and routed through Agent observations | `cargo test -p backend-rust mcp_ agent_runtime_routes_mcp_tools_through_audited_observation_path --offline` |
| Guardian approval review | `novex-approval-review` owns Guardian risk/policy/user-authorization vocabulary, fail-closed policy review decisions, repeated-denial breaker behavior, compact transcript/action prompt, strict model assessment parsing, and model failure decisions; `guardian_review` is a first-class LLM route purpose; backend approval pauses can call the configured Guardian route when `auto_approve` is enabled and include serialized reviewer metadata; reviewed Guardian approvals can continue deterministic and model-loop tool execution without human pause; trace/eval preserve both approval-pause and auto-approved action evidence | `cargo test -p novex-model guardian_review --offline && cargo test -p novex-approval-review guardian --offline && cargo test -p novex-approval-review guardian_model_review --offline && cargo test -p backend-rust guardian_review_model_route --offline && cargo test -p backend-rust guardian_auto_approval --offline && cargo test -p backend-rust guardian_model_review --offline && cargo test -p backend-rust guardian_review --offline && cargo test -p novex-eval guardian_auto_approval --offline && cargo test -p novex-eval guardian_model_review --offline && cargo test -p novex-eval guardian_review --offline` |
| Rollout/trace/eval | Agent events convert to trace bundles, preserve runtime and inference spans with latency/usage/cost/error/retry/fallback/circuit-breaker/supervisor/remote-compaction/model_delta evidence, replay via API, persist `ai_rollout`, capture eval candidates with runtime/model/model_delta/provider-error/provider-retry/provider-fallback/provider-circuit/compaction/runtime-supervisor/remote-compaction tags, expose tenant-scoped breaker controls and model ops summary aggregation, persist model health checks, seed health automation, maintain active model ops alerts, expose active alerts in ops summary for dashboards, deliver active alerts through audited Feishu/dry-run scheduler bridge, and score `trace_replay` eval runs | `cargo test -p novex-trace runtime_span --offline && cargo test -p novex-trace inference_span --offline && cargo test -p backend-rust runtime_spans --offline && cargo test -p backend-rust runtime_supervisor --offline && cargo test -p backend-rust inference_spans --offline && cargo test -p backend-rust inference_cost --offline && cargo test -p backend-rust provider_error --offline && cargo test -p backend-rust provider_retry --offline && cargo test -p backend-rust provider_lifecycle --offline && cargo test -p backend-rust provider_lifecycle_trace --offline && cargo test -p backend-rust route_circuit_breaker --offline && cargo test -p backend-rust persistent_route_circuit_breaker --offline && cargo test -p backend-rust route_breaker_controls --offline && cargo test -p backend-rust model_circuit_breaker_ --offline && cargo test -p backend-rust model_ops_summary --offline && cargo test -p backend-rust model_health_persistence --offline && cargo test -p backend-rust model_health_automation --offline && cargo test -p backend-rust model_health_alert --offline && cargo test -p backend-rust model_health_check_key --offline && cargo test -p backend-rust model_ops_alert_delivery --offline && cargo test -p backend-rust model_alert_delivery_key --offline && cargo test -p backend-rust route_circuit_breaker_trace --offline && cargo test -p backend-rust multi_hop_fallback --offline && cargo test -p backend-rust model_route_retry_policy --offline && cargo test -p backend-rust model_runtime_retry_policy --offline && cargo test -p backend-rust model_route_fallback --offline && cargo test -p backend-rust model_inference_error --offline && cargo test -p backend-rust model_delta_spans --offline && cargo test -p novex-eval runtime_spans --offline && cargo test -p novex-eval runtime_supervisor --offline && cargo test -p novex-eval remote_compaction --offline && cargo test -p novex-eval inference_spans --offline && cargo test -p novex-eval model_delta --offline && cargo test -p novex-eval provider_error_spans --offline && cargo test -p novex-eval provider_retry_spans --offline && cargo test -p novex-eval provider_fallback --offline && cargo test -p novex-eval circuit_breaker --offline && cargo test -p novex-eval compaction --offline && cargo test -p backend-rust model_chat_cost_cents --offline && cargo test -p backend-rust model_chat_response_cost_runtime --offline && cargo test -p backend-rust model_chat_response_extracts_answer_usage_and_route_summary --offline && cargo test -p backend-rust model_chat_usage_record --offline && cargo test -p backend-rust agent_run_events_convert_to_trace_bundle --offline && cargo test -p backend-rust eval_case_capture --offline && cargo test -p backend-rust eval_runtime_normalizes_trace_replay_run_mode --offline` |
| Full Rust workspace | Rust crates and backend remain coherent after the agent foundation slices | `cargo fmt -- --check && cargo test --workspace --offline` |

## Follow-up Implementation Plans

- MCP gateway: `docs/plans/2026-06-16-agent-mcp-gateway.md`
- Rollout, trace, replay, eval: `docs/plans/2026-06-16-agent-rollout-eval.md`
- Agent runtime trace spans: `docs/plans/2026-06-17-agent-runtime-trace-spans.md`
- Agent runtime supervisor: `docs/plans/2026-06-17-agent-runtime-supervisor.md`
- Agent remote compaction: `docs/plans/2026-06-17-agent-remote-compaction.md`
- Agent Guardian approval review: `docs/plans/2026-06-17-agent-guardian-review.md`
- Agent Guardian model review: `docs/plans/2026-06-17-agent-guardian-model-review.md`
- Agent Guardian auto approval: `docs/plans/2026-06-17-agent-guardian-auto-approval.md`
- Agent event stream: `docs/plans/2026-06-17-agent-event-stream.md`
- Agent event WebSocket transport: `docs/plans/2026-06-17-agent-event-websocket.md`
- Agent event WebSocket browser auth: `docs/plans/2026-06-17-agent-event-websocket-browser-auth.md`
- Agent model compaction: `docs/plans/2026-06-17-agent-model-compaction.md`
- Agent inference trace spans: `docs/plans/2026-06-17-agent-inference-trace-spans.md`
- Agent inference cost trace: `docs/plans/2026-06-17-agent-inference-cost-trace.md`
- Agent provider error trace: `docs/plans/2026-06-17-agent-provider-error-trace.md`
- Agent provider retry trace: `docs/plans/2026-06-17-agent-provider-retry-trace.md`
- Agent route fallback lifecycle: `docs/plans/2026-06-17-agent-route-fallback-lifecycle.md`
- Agent route circuit breaker: `docs/plans/2026-06-17-agent-route-circuit-breaker.md`
- Agent route multi-hop fallback: `docs/plans/2026-06-17-agent-route-multi-hop-fallback.md`
- Agent persistent route circuit breaker: `docs/plans/2026-06-17-agent-persistent-route-circuit-breaker.md`
- Agent route breaker controls: `docs/plans/2026-06-17-agent-route-breaker-controls.md`
- Agent model ops summary: `docs/plans/2026-06-17-agent-model-ops-summary.md`
- Agent model health persistence: `docs/plans/2026-06-17-agent-model-health-persistence.md`
- Agent model health automation: `docs/plans/2026-06-17-agent-model-health-automation.md`
- Agent model ops alert summary: `docs/plans/2026-06-17-agent-model-ops-alert-summary.md`
- Agent model ops alert delivery: `docs/plans/2026-06-17-agent-model-ops-alert-delivery.md`
- Notebook workspace: `docs/plans/2026-06-16-notebook-workspace.md`
- Customer service agent: `docs/plans/2026-06-16-customer-service-agent.md`
- Agent run cancellation checkpoints: `docs/plans/2026-06-17-agent-run-cancellation.md`
- Agent runtime registry: `docs/plans/2026-06-17-agent-runtime-registry.md`
- Agent background run queue: `docs/plans/2026-06-17-agent-background-run-queue.md`
- Agent queued model loop: `docs/plans/2026-06-17-agent-queued-model-loop.md`
- Agent queue cancel sync: `docs/plans/2026-06-17-agent-queue-cancel-sync.md`
- Agent queue resume requeue: `docs/plans/2026-06-17-agent-queue-resume-requeue.md`
- Agent queue broker wake-up: `docs/plans/2026-06-17-agent-queue-broker-wakeup.md`
- Agent queue broker consumer: `docs/plans/2026-06-17-agent-queue-broker-consumer.md`
- Agent queue outbox publisher: `docs/plans/2026-06-17-agent-queue-outbox-publisher.md`
- Agent POC configured model route: `docs/plans/2026-06-17-agent-poc-configured-model-route.md`
- Agent POC live smoke: `docs/plans/2026-06-17-agent-poc-live-smoke.md`
- Agent provider-native remote compaction transport: `docs/plans/2026-06-17-agent-provider-native-remote-compaction-transport.md`
- Agent cross-process provider abort: `docs/plans/2026-06-17-agent-cross-process-provider-abort.md`
- Agent provider compact response transport: `docs/plans/2026-06-17-agent-provider-compact-response-transport.md`
- Agent provider compact unary transport: `docs/plans/2026-06-17-agent-provider-compact-unary.md`
- Agent provider background response capture: `docs/plans/2026-06-17-agent-provider-background-response-capture.md`
- Agent provider native cancel control: `docs/plans/2026-06-17-agent-provider-native-cancel.md`
- Agent provider call lease: `docs/plans/2026-06-17-agent-provider-call-lease.md`
- Agent provider call lease controls: `docs/plans/2026-06-17-agent-provider-call-lease-controls.md`
- Agent ResponseItem history projection: `docs/plans/2026-06-17-agent-response-item-history.md`
- Agent ResponseItem ledger: `docs/plans/2026-06-17-agent-response-item-ledger.md`
- Agent provider call lease heartbeat: `docs/plans/2026-06-17-agent-provider-call-lease-heartbeat.md`
- Agent provider embedding/rerank lease: `docs/plans/2026-06-17-agent-provider-embedding-rerank-lease.md`
- Agent provider media lease: `docs/plans/2026-06-17-agent-provider-media-lease.md`
- Agent model delta trace/eval preservation: `docs/plans/2026-06-17-agent-model-delta-trace-eval.md`
- Agent frontend model delta rendering: `docs/plans/2026-06-17-agent-frontend-model-delta-rendering.md`
- Agent provider Responses output text delta: `docs/plans/2026-06-17-agent-provider-responses-output-text-delta.md`
- Agent provider Responses transport selection: `docs/plans/2026-06-17-agent-provider-responses-transport-selection.md`
- Agent streaming tool-call parser: `docs/plans/2026-06-17-agent-streaming-tool-call-parser.md`
- Agent backend streaming tool-call detection: `docs/plans/2026-06-17-agent-streaming-tool-call-backend.md`
- Agent streaming tool-call decision adoption: `docs/plans/2026-06-17-agent-streaming-tool-call-decision.md`
- Agent streaming tool-call early stop: `docs/plans/2026-06-17-agent-streaming-tool-call-early-stop.md`
- Agent stream response id capture: `docs/plans/2026-06-17-agent-stream-response-id-capture.md`
- Agent stream native cancel: `docs/plans/2026-06-17-agent-stream-native-cancel.md`
- Agent stream-native runtime API: `docs/plans/2026-06-17-agent-stream-native-runtime-api.md`
- Agent stream completion builder: `docs/plans/2026-06-17-agent-stream-completion-builder.md`
- Agent stream call lifecycle: `docs/plans/2026-06-17-agent-stream-call-lifecycle.md`
- Agent stream transport task: `docs/plans/2026-06-17-agent-stream-transport-task.md`
- Agent stream transport executor: `docs/plans/2026-06-17-agent-stream-transport-executor.md`
- Agent provider stream dispatch: `docs/plans/2026-06-17-agent-provider-stream-dispatch.md`
- Agent provider HTTP transport adapter: `docs/plans/2026-06-17-agent-provider-http-transport-adapter.md`
- Agent provider response transport adapter: `docs/plans/2026-06-17-agent-provider-response-transport-adapter.md`
- Agent provider native cancel transport adapter: `docs/plans/2026-06-17-agent-provider-native-cancel-transport-adapter.md`
- Agent provider client native cancel dispatch: `docs/plans/2026-06-17-agent-provider-client-native-cancel-dispatch.md`
- Agent provider RAG transport adapter: `docs/plans/2026-06-17-agent-provider-rag-transport-adapter.md`
- Agent provider media transport adapter: `docs/plans/2026-06-17-agent-provider-media-transport-adapter.md`
- Agent provider client media dispatch: `docs/plans/2026-06-18-agent-provider-client-media-dispatch.md`
- Agent provider client chat response parsers: `docs/plans/2026-06-18-agent-provider-client-chat-response-parsers.md`
- Agent provider client chat dispatch: `docs/plans/2026-06-18-agent-provider-client-chat-dispatch.md`
- Agent provider client chat request planning: `docs/plans/2026-06-18-agent-provider-client-chat-request-plan.md`
- Agent provider client focused modules: `docs/plans/2026-06-18-agent-provider-client-focused-modules.md`
- Agent tool executor registry: `docs/plans/2026-06-18-agent-tool-executor-registry.md`
- Agent backend tool executor registry dispatch: `docs/plans/2026-06-18-agent-tool-executor-registry-dispatch.md`
- Agent tool executor dispatch plan: `docs/plans/2026-06-18-agent-tool-executor-dispatch-plan.md`
